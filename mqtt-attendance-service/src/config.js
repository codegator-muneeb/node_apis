const HOST = 'mqtt://www.syncme.io';
const CLIENT = 'mqttjs01';
const USER = "Zerowav";
const PWD = "zw@pune";
const PORT = 1883;
const FRAME_SIZE = 2;
const QOS = 2;
const DBHOST = 'localhost';
const DB = 'entry_point_db';
const DBPWD = 'muneeb6696';
const DBPORT = 5432;
const TIMEOUT = 10000;

module.exports = {
    HOST,
    CLIENT,
    USER,
    PWD,
    PORT,
    FRAME_SIZE,
    DBHOST,
    DB,
    DBPWD,
    DBPORT,
    QOS,
    TIMEOUT
};